{
  "name": "risksense-monorepo",
  "version": "1.0.0",
  "description": "RiskSense: Autonomous Agentic Commerce & Real-Time AI Risk Gateway for Razorpay",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm --prefix frontend run build",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend && npm --prefix frontend run build",
    "start": "node backend/src/server.js",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev"
  },
  "keywords": ["razorpay", "buildathon", "agentic-commerce", "ap2", "risk-manager"],
  "author": "RiskSense Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
